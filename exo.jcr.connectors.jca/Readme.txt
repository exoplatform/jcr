If you want to build JCA module for :
 * JCR standalone :  run "mvn clean install" command.
 * Gatein : run "mvn clean install -Pgatein" command.
 * eXo platform : run "mvn clean install -Pplatform" command.